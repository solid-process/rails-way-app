<%= render("web/user/settings/header") %>

<h2>API token</h2>

<pre><%= cookies.encrypted[:user_token] || Current.user.token.value %></pre>

<%= button_to("Refresh", user_settings_token_path, {
  method: :put,
  data: {confirm: 'Are you sure to generate a new API token?'}
}) %>
